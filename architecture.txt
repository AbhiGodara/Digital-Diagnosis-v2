## Revised Architecture — Clean Version
```
User Input (free text + age + gender)
            │
            ▼
┌─────────────────────────┐
│   LLM CALL #1           │
│   Symptom Extraction    │
│                         │
│   Input: user text      │
│   GET  /api/symptoms    │
│   (list all 84 symptoms)│
│   Output: matched       │
│           symptom list  │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│  Binary Vector Builder  │
│  matched list → 84-dim  │
│  binary vector          │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│   LightGBM Classifier   │
│                         │
│   Input: binary vector  │
│   Output: top 3         │
│   diseases + probs      │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│  Knowledge Base Lookup  │
│  Direct JSON lookup for │
│  each of top 3 diseases │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│   LLM CALL #2           │
│   Explanation Generator │
│                         │
│   Input: predictions    │
│         + disease info  │
│         + user text     │
│         + patient info  │
│   Output: explanation   │
│           + advice      │
│           + disclaimer  │
└──────────┬──────────────┘
           │
           ▼
    Streamlit Display